[project]
name = "ai-chat-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
]
[tool.fastapi]
entrypoint = "backend.main:app"